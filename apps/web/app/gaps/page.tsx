'use client';
import { useEffect,useState } from 'react';
import { AppShell } from '@/components/AppShell';import { StatusChip } from '@/components/StatusChip';import { ActionItem,browserApiUrl,demoHeaders,fallbackDashboard } from '@/lib/api';
const priorityAr={critical:'حرج',high:'عالٍ',medium:'متوسط',low:'منخفض'};const statusAr:Record<string,string>={open:'مفتوح',planned:'مخطط',in_progress:'قيد التنفيذ',blocked:'متعثر',pending_evidence:'بانتظار دليل',pending_review:'بانتظار مراجعة',completed:'مكتمل',accepted_risk:'مخاطرة مقبولة'};
export default function Gaps(){const [items,setItems]=useState<ActionItem[]>(fallbackDashboard.actions);const [live,setLive]=useState(false);const [updating,setUpdating]=useState<string|null>(null);useEffect(()=>{const controller=new AbortController();fetch(`${browserApiUrl()}/v1/actions`,{headers:demoHeaders,signal:controller.signal}).then(r=>{if(!r.ok)throw new Error();return r.json() as Promise<ActionItem[]>}).then(data=>{setItems(data);setLive(true)}).catch(()=>{});return()=>controller.abort()},[]);async function complete(item:ActionItem){setUpdating(item.id);try{const response=await fetch(`${browserApiUrl()}/v1/actions/${item.id}`,{method:'PATCH',headers:demoHeaders,body:JSON.stringify({status:'completed'})});if(!response.ok)throw new Error();const updated=await response.json() as ActionItem;setItems(current=>current.map(x=>x.id===updated.id?updated:x))}finally{setUpdating(null)}}const open=items.filter(x=>x.status!=='completed');const critical=open.filter(x=>x.priority==='critical').length;const completed=items.filter(x=>x.status==='completed').length;return <AppShell title="الفجوات وخطط المعالجة"><div className="mb-4 flex justify-end"><StatusChip tone={live?'emerald':'amber'}>{live?'متصل بالـ API':'بيانات عرض احتياطية'}</StatusChip></div><div className="grid gap-4 md:grid-cols-4"><div className="card p-5"><b className="text-3xl">{items.length}</b><p className="text-sm text-slate-500">إجمالي الإجراءات</p></div><div className="card p-5"><b className="text-3xl text-red-700">{critical}</b><p className="text-sm text-slate-500">حرجة مفتوحة</p></div><div className="card p-5"><b className="text-3xl text-amber-700">{open.length}</b><p className="text-sm text-slate-500">قيد المعالجة</p></div><div className="card p-5"><b className="text-3xl text-emerald-700">{items.length?Math.round(completed/items.length*100):0}%</b><p className="text-sm text-slate-500">تقدم الإغلاق</p></div></div><div className="card mt-5 overflow-x-auto"><table className="w-full min-w-[820px] text-right text-sm"><thead className="bg-slate-50 text-slate-500"><tr>{['الإجراء','المالك','المخاطرة','الأطر المتأثرة','الحالة',''].map((x,i)=><th className="px-5 py-3" key={`${x}-${i}`}>{x}</th>)}</tr></thead><tbody className="divide-y">{items.map(a=><tr key={a.id}><td className="px-5 py-4 font-bold">{a.title}<div className="text-xs font-normal text-slate-400" dir="ltr">{a.due_date}</div></td><td className="px-5 py-4">{a.owner}</td><td className="px-5 py-4"><StatusChip tone={a.priority==='critical'?'red':'amber'}>{priorityAr[a.priority]}</StatusChip></td><td className="px-5 py-4">{a.impacted_frameworks.length}</td><td className="px-5 py-4">{statusAr[a.status]??a.status}</td><td className="px-5 py-4">{a.status!=='completed'&&live?<button disabled={updating===a.id} onClick={()=>complete(a)} className="rounded-lg border border-emerald-700 px-3 py-2 text-xs font-black text-emerald-700 disabled:opacity-50">{updating===a.id?'جارٍ...':'إغلاق'}</button>:null}</td></tr>)}</tbody></table></div></AppShell>}
