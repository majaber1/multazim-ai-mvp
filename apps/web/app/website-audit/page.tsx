'use client';
import { useState } from 'react';
import { AppShell } from '@/components/AppShell';
export default function WebsiteAudit(){const [url,setUrl]=useState('');const [done,setDone]=useState(false);return <AppShell title="فحص الموقع"><div className="card p-7"><h2 className="text-2xl font-black">أدخل رابط موقعك أو متجرك</h2><p className="mt-2 text-slate-500">سنفحص الصفحات الأساسية وعناصر الثقة بشكل أولي.</p><div className="mt-6 flex gap-3"><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://example.sa" className="min-w-0 flex-1 rounded-2xl border border-slate-300 px-5 py-4 outline-none focus:border-emerald-500"/><button onClick={()=>setDone(true)} className="rounded-2xl bg-emerald-600 px-6 font-black text-white">ابدأ الفحص</button></div></div>{done&&<div className="mt-6 grid gap-4 md:grid-cols-2">{[['سياسة الخصوصية','غير موجودة','red'],['سياسة الاسترجاع','موجودة','green'],['بيانات التواصل','موجودة','green'],['إشعار ملفات الارتباط','بحاجة تحسين','amber']].map(([t,s,c])=><div key={t} className="card flex items-center justify-between p-5"><div className="font-black">{t}</div><span className={`badge ${c==='red'?'bg-red-100 text-red-700':c==='green'?'bg-emerald-100 text-emerald-700':'bg-amber-100 text-amber-700'}`}>{s}</span></div>)}</div>}</AppShell>}
