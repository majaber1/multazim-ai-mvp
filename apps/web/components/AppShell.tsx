'use client';
import Link from 'next/link';
import { Sidebar } from './Sidebar';
import { useLocale } from './LocaleProvider';
export function AppShell({children,title,subtitle='شركة آفاق الرقمية السعودية — بيانات تجريبية'}:{children:React.ReactNode;title:string;subtitle?:string}){const {locale,setLocale,t}=useLocale();return <div className="flex min-h-screen bg-[#f5f7f6]"><Sidebar/><main className="min-w-0 flex-1"><header className="sticky top-0 z-40 border-b border-slate-200 bg-white/90 px-5 py-4 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between"><div><h1 className="text-xl font-black md:text-2xl">{t(title)}</h1><p className="mt-1 text-xs text-slate-500">{t(subtitle)}</p></div><div className="flex items-center gap-2"><button onClick={()=>setLocale(locale==='ar'?'en':'ar')} aria-label="Switch language" className="rounded-xl border border-slate-200 px-3 py-2 text-xs font-bold">{locale==='ar'?'EN':'عربي'}</button><Link href="/universe" className="rounded-xl bg-emerald-700 px-4 py-2 text-xs font-black text-white">{t('تحديث نطاق الامتثال')}</Link></div></div></header><div className="mx-auto max-w-7xl p-5 md:p-7">{children}</div></main></div>}
